# 2026-08-22 工作日志

## 鸭鸭日记本 — 项目系统评估（gsd）

用户以 `/gsd` 发起系统评估。发现项目已存在 `docs/project-assessment.md`（8-16 由前一会话生成，覆盖完成度对照清单/六维评分/问题根因/改进路线图）。

**做法**：逐项核实现有评估报告的准确性（grep 确认 main.py 572 行单文件、无 insight_notes、无 audio_ref、无 ASR/TTS provider 抽象、无 logging 均属实），而非盲目重写。

**发现遗漏点**：现有报告遗漏了「文档过时」问题——`README.md` 仍是"CrewAI + DeepSeek 模板"描述（标题/目录结构/快速开始均未反映鸭鸭日记本应用），根目录遗留 `overview.md`（早期模板总结）、`prototype/duck-diary-core-interaction.html`（早期原型）、`scripts/test_connection.py`（早期连接测试）。

**产出**：更新评估报告（commit ac8fb5d），补充 P10「README 过时 + 遗留文件混乱」+ A7「重写 README + 清理遗留文件」+ 根因④「文档未随迭代同步更新」。

**评估结论**（现有报告核心，已确认准确）：总体完成度约 78%，综合评分 6.4/10。核心链路完整可验收；短板为功能细节（insight 落库/请假补位/幼儿对比/语音抽象）、代码结构（main.py 单文件 572 行）、测试与文档一致性。

## 语音体验改进（人工验收补充）

用户从人工验收角度补充 2 点，均判定可短期修复，已实施（commit dead0d4）：

1. **非 LLM 步骤语音引导**：原仅 LLM 回复有语音，现进入首页/选人/开场/结束均语音播报。实现：把 `speak()` 从 `startConversation` 局部函数提升为全局，在 `renderPicker`/`showOpening`/`showEnding` 末尾调用。注意浏览器自动播放限制（首次无交互可能被拦，点击后正常）。
2. **语音拟人化（低成本）**：`speak()` 增加 `_pickVoice()`（优先 Google/Neural/Xiaoxiao/Yunxi/Tingting 等自然中文音色）+ 参数优化（rate 0.95 稍慢、pitch 1.2 略高）+ 录音前 `stopSpeak()` 静音避免干扰识别。

**中期方案（未实施，待确认）**：Edge-TTS 后端合成，拟人感提升最显著，但需加后端依赖+接口。已记入评估报告 B7。
